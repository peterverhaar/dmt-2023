15
SELECT YEAR, COUNT(*)
FROM BOOK
GROUP BY YEAR

16
SELECT DESCRIPTION, AVG(SOLD)
FROM BOOK
LEFT JOIN GENRE
ON BOOK.GENRE=GENRE.GENRE_ID
GROUP BY DESCRIPTION

17
SELECT LAST_NAME, FIRST_NAME, SUM(SOLD)
FROM BOOK
LEFT JOIN AUTHOR
ON BOOK.AUTHOR=AUTHOR.AUTHOR_ID
GROUP BY AUTHOR

18
SELECT LAST_NAME, FIRST_NAME, COUNT(*)
FROM AUTHOR
LEFT JOIN BOOK
ON AUTHOR.AUTHOR_ID=BOOK.AUTHOR
GROUP BY AUTHOR
HAVING COUNT(AUTHOR) > 1

19
SELECT NAME, COUNT(*)
FROM BOOK
LEFT JOIN PUBLISHER
ON BOOK.PUBLISHER=PUBLISHER.PUBL_ID
GROUP BY PUBLISHER
HAVING COUNT(PUBLISHER) > 3

20
SELECT DESCRIPTION, MAX(SOLD)
FROM BOOK
LEFT JOIN GENRE
ON BOOK.GENRE = GENRE.GENRE_ID
GROUP BY GENRE